import React from 'react';
import './Spinner.css'; // Or wherever your styles are

const Spinner = () => (
  <div className="loader-container">
    <div className="loader"></div>
  </div>
);

export default Spinner;